📚 Звіт до Лабораторної Роботи №01
🏷️ Тема Роботи
Вступні заняття, налаштування середовища, початок роботи з Python та Markdown.

🎯 Мета Роботи
Налаштувати інтегроване середовище розробки VS Code, створити та інтегрувати локальний репозиторій з GitHub, написати першу програму на Python з демонстрацією принципів ООП, а також створити та оформити звіт про виконання роботи з використанням мови розмітки Markdown.

⚙️ Порядок Виконання Роботи та Результати
1. Налаштування Середовища VS Code та Інструментів
Встановлено інтегроване середовище розробки (IDE) VS Code.

Встановлено необхідні плагіни:

Python (для підтримки синтаксису та налагодження).

Jupyter (для роботи з інтерактивними ноутбуками .ipynb).

GitHub Copilot (як інструмент для прискорення написання коду).

Створено локальний репозиторій та синхронізовано його з GitHub для контролю версій.

2. Перша Програма на Python (Вступна Демонстрація)
Створено файл intro.py для перевірки базового функціоналу Python, роботи з рядковими форматуваннями та використання вбудованого модуля datetime.

Python

from datetime import datetime

name = "Vadym"
location = "Lviv"

print(f"{name} start programming at {datetime.now()}. {location} is the best city!")
Отриманий результат виконання:

Vadym start programming at 2025-10-26 16:41:37.170949. Lviv is the best city!

3. Робота з Jupyter Notebook та Перша ООП Демонстрація
Створено інтерактивний файл 1.ipynb.

У ньому використано комірки Markdown для оформлення тексту та комірки Code для виконання коду.

Створено файл oop_demo.py, що демонструє ключові концепції Об'єктно-орієнтованого програмування (ООП): Клас, Об'єкт та Інкапсуляція.

Код Демонстрації ООП (Клас Book):

Python

class Book:
    """Клас, що представляє книгу."""

    def __init__(self, title, author, year):
        """Конструктор (ініціалізатор) класу."""
        self._title = title     # Інкапсульований атрибут
        self._author = author
        self._is_read = False

    def mark_as_read(self):
        """Метод для зміни стану (поведінка)."""
        if not self._is_read:
            self._is_read = True

    def display_info(self):
        """Метод для відображення інформації про об'єкт."""
        read_status = "Прочитана" if self._is_read else "Не прочитана"
        print(f"Назва: {self._title}, Автор: {self._author}, Статус: {read_status}")

# Створення об'єктів та виклик методів
book1 = Book("Чистий код", "Роберт Мартін", 2008)
book1.display_info()
book1.mark_as_read() 
book1.display_info()
Результат виконання:

Назва: Чистий код, Автор: Роберт Мартін, Статус: Не прочитана
Назва: Чистий код, Автор: Роберт Мартін, Статус: Прочитана
💡 Висновок
В рамках цієї роботи було успішно налаштовано повноцінне середовище розробки для подальшого вивчення та виконання робіт з Об'єктно-орієнтованого програмування.

Ключові набуті навички:

Конфігурація VS Code та інструментів розробки (Python, Jupyter, Copilot).

Робота з Git та GitHub для професійного контролю версій.

Оформлення технічної документації за допомогою синтаксису Markdown.

Написання першої програми, яка демонструє ключові концепції ООП: створення класу (Book), створення об'єкта (book1) та використання інтерфейсних методів для керування станом (Інкапсуляція).

Усі поставлені цілі досягнуті, що створює міцну основу для подальшого поглибленого вивчення принципів ООП.