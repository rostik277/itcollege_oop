🐍 Звіт до Лабораторної Роботи №02: Основи Python
🏷️ Тема Роботи
Основи програмування на Python: Ключові конструкції та структури даних.

🎯 Мета Роботи
Навчитись застосовувати основні конструкції мови Python, виконати приклади, що демонструють типи даних, керуючі конструкції та особливості синтаксису, а також закріпити навички роботи з інструментами розробки.

💻 Хід Роботи та Приклади Коду
1. Типи Даних та Структури
Мета: Демонстрація стандартних примітивних та складених типів даних Python.

Python

student_name = "Олександр"  # str (Рядок)
student_age = 20           # int (Ціле число)
gpa = 3.85                 # float (Число з плаваючою комою)

courses = ["Захист України", "Комп'ютерні мережі", "Диференціальні рівняння"] # list (Список)
contact_info = {"email": "student@example.com", "group": "KN-31"} # dict (Словник)
student_id = ("Серія КН", 12345678) # tuple (Кортеж)
skills = {"Python", "Networking", "Git", "Python"} # set (Набір) - автоматично унікальний
2. Вбудовані Константи та Зарезервовані Слова
Мета: Вивчення спеціальних значень (None, True, False) та зарезервованих слів, що мають особливе значення в мові.

Python

import keyword # Імпорт модуля для демонстрації ключових слів

last_grade = None # None - відсутність значення
is_active = False # False - булева константа

print(f"Остання отримана оцінка: {last_grade}")
print(f"Студент активний: {is_active}")

print("\n--- Зарезервовані слова Python ---")
# Відображення усіх зарезервованих слів
print(keyword.kwlist)
3. Вбудовані Функції (Built-in Functions)
Мета: Демонстрація ефективного використання стандартних функцій для обробки колекцій.

Python

grades = [85, 92, 78, 95, 88]

print(f"Список оцінок: {grades}")
print(f"Найвища оцінка: {max(grades)}") # Функція max()
print(f"Найнижча оцінка: {min(grades)}") # Функція min()
print(f"Сума всіх оцінок: {sum(grades)}") # Функція sum()
# Комбінація функцій для розрахунку середнього значення
print(f"Середній бал: {sum(grades) / len(grades)}")
4. Цикли (for та while)
Мета: Реалізація ітерації по колекціях та виконання блоку коду на основі умови.

Python

# 1. Цикл for...in (Ітерація по елементах списку)
devices = ["Router", "Switch", "Firewall", "PC"]
print("--- Мережеві пристрої ---")
for device in devices:
    print(f"Обробка пристрою: {device}")

# 2. Цикл while (Виконання, доки умова істинна)
print("\n--- Початок відліку ---")
counter = 5
while counter > 0:
    print(f"Залишилось {counter} сек...")
    counter -= 1 
print("Тест завершено!")
5. Розгалуження (if/elif/else)
Мета: Реалізація умовного виконання коду (логіка прийняття рішень).

Python

import random
# Згенеруємо випадкову оцінку
score = random.randint(40, 100)

print(f"Ваш бал: {score}")

if score >= 90:
    print("Оцінка: Відмінно (A)")
elif score >= 75:
    print("Оцінка: Добре (B)")
elif score >= 60:
    print("Оцінка: Задовільно (C)")
else:
    print("Оцінка: Незадовільно (F). Потрібне перескладання.")
6. Обробка Винятків (try...except...finally)
Мета: Демонстрація механізму обробки помилок для підвищення стійкості програми.

Python

my_list = [10, 20, 30]
index_to_check = 5 # Спровокуємо IndexError

try:
    print(f"Намагаємось отримати елемент з індексом {index_to_check}...")
    value = my_list[index_to_check]
    print(f"Значення: {value}")
except IndexError as e:
    print(f"ПОМИЛКА: Індекс вийшов за межі списку!")
    print(f"Деталі помилки: {e}")
finally:
    # Блок 'finally' виконується незалежно від наявності помилки
    print(f"Перевірку списку завершено. Довжина списку: {len(my_list)}")
7. Контекст-менеджер (with open)
Мета: Демонстрація безпечної та автоматичної роботи з ресурсами (наприклад, файлами).

Python

data_to_write = [
    "Це перша лінія звіту.\n",
    "Це друга лінія.\n",
    "Кінець звіту.\n"
]

# 'with' гарантує, що файл буде закрито автоматично
try:
    with open("my_report.txt", "w", encoding="utf-8") as f:
        print("Відкриваємо файл для запису...")
        f.writelines(data_to_write)
        print("Дані успішно записано (у файл 'my_report.txt').")
except Exception as e:
    print(f"Не вдалося записати файл: {e}")

print("Роботу з файлом завершено.")
8. Лямбда-функції (lambdas)
Мета: Демонстрація створення невеликих анонімних функцій для швидких операцій, зокрема, як функцій-ключів.

Python

# Звичайна функція
def square_func(x):
    return x * x

# Анонімна функція (lambda)
square_lambda = lambda x: x * x

print(f"Функція: {square_func(5)}")
print(f"Лямбда: {square_lambda(5)}")

print("\n--- Використання лямбди для сортування ---")
students = [
    {"name": "Іван", "age": 20},
    {"name": "Марія", "age": 19},
    {"name": "Петро", "age": 21}
]

# Сортуємо список, використовуючи лямбду як ключ сортування
sorted_students = sorted(students, key=lambda s: s['age'])

print(f"Список після сортування за віком: {sorted_students}")
💡 Висновок
Під час виконання цієї лабораторної роботи було успішно закріплено знання основних конструкцій мови Python.

Зокрема, було набуто навичок використання:

Різноманітних типів даних (списки, словники, набори).

Вбудованих функцій (max, min, sum, len), які значно підвищують ефективність роботи з даними. (Як ви зазначили, вони реально корисні!)

Керуючих конструкцій (if/elif/else, for, while) для управління логікою програми.

Механізмів обробки винятків (try...except) та контекст-менеджерів (with) для створення надійного коду.

Це створює міцну функціональну базу, необхідну для подальшого вивчення та ефективного застосування парадигми Об'єктно-орієнтованого програмування.